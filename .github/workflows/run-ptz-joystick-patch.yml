name: Run physical PTZ joystick patch

on:
  push:
    branches: [feature/physical-ptz-joystick]
    paths:
      - .github/workflows/run-ptz-joystick-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/physical-ptz-joystick
      - name: Apply patch
        run: python tools/apply_ptz_joystick_patch.py
      - name: Syntax check
        run: node --check custom_components/webrtc/www/webrtc-camera.js
      - name: Commit update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add custom_components/webrtc/www/webrtc-camera.js custom_components/webrtc/manifest.json
          git commit -m "Add proportional physical PTZ joystick"
          git push origin feature/physical-ptz-joystick
